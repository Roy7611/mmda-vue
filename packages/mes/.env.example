# Unified gateway. All services (oauth / base / mes) go through this origin.
VITE_BASE_API=http://127.0.0.1:8001/api

# OAuth client credentials for password grant (do not commit real secrets)
VITE_OAUTH_CLIENT_ID=mmda-mes
VITE_OAUTH_CLIENT_SECRET=
