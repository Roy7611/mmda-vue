# logic/validators/

- **层**：Logic
- **源码**：packages/core/src/logic/validators/

## 职责

`FieldValidator`、注册表、按类型实现的内置校验器。`field_logic` 只组装，不写 Min/Email。

目录：`types.ts`、`registry.ts`、`number.ts`、`datetime.ts`、`string.ts`、`collection.ts`。
