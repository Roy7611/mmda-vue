---
description: MMDA 字段引用只有 enum / ref / hasOne 三种，下拉与筛选必须走 valueOf/labelOf
alwaysApply: true
---

# 字段引用（enum / ref / hasOne）

`MetaUiField.reference` 只有三种，取值/显示只走 `valueOf` / `labelOf`，主键唯一，不要对选项 `getDistinct`。

| 类型 | 选项从哪来 | 主键 | 说明 |
|------|------------|------|------|
| **enum** | 本地 `refOptions`（`value;code;label` 解析成对象） | `valueOf` → code（英文成员） | `labelOf` → label（显示文本） |
| **ref** | 首页 50 整对象写入 `refOptions`；`pagination` 未穷尽则搜索远程 | `id`（`refFlds[0]`） | 不要 editor 猜，不要当前表 `pivotValues` |
| **hasOne** | 与 ref 同一套首页 50；未穷尽搜索远程 | `id` | 不是整张大表灌进去；搜索页不覆盖首页 |

表单下拉和列表 CheckBox 筛选同一套取值规则：显示 `labelOf`，提交/过滤用 `valueOf`。穷尽（没有下一页）才本地滤。搜索结果只给当前列表，不要 `splice` 掉首页那 50 条。

不要：把列做成 EJ2 外键列、`itemTemplate` 猜字段、`mmdaFilterLabel`、从当前页行 distinct 凑选项、用 editor 名猜能不能全量。
